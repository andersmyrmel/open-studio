# Open-Studio Environment Configuration
# Copy this file to .env and update with your values

# ============================================
# PostgreSQL Database Connection
# ============================================

# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
#
# Examples:
# - Local database: postgresql://postgres:password@localhost:5432/mydb
# - Docker host: postgresql://postgres:password@host.docker.internal:5432/mydb
# - Remote database: postgresql://user:password@db.example.com:5432/production_db
# - With SSL: postgresql://user:password@host:5432/db?sslmode=require
#
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# ============================================
# Application Configuration
# ============================================

# Port for the web server (default: 3000)
PORT=3000

# Host port for Docker Compose (maps to container port 3000)
HOST_PORT=8080

# Node environment (development, production, test)
NODE_ENV=development

# ============================================
# Optional Configuration
# ============================================

# Disable Next.js telemetry (recommended)
NEXT_TELEMETRY_DISABLED=1

# ============================================
# Notes
# ============================================

# Security:
# - Never commit .env file to version control
# - Use strong passwords for database connections
# - Consider using SSL for remote connections
# - Restrict database user permissions appropriately

# Docker:
# - Use host.docker.internal instead of localhost when connecting from Docker
# - Or use the service name if database is also in docker-compose

# Troubleshooting:
# - If connection fails, verify PostgreSQL is running
# - Check firewall rules for database port
# - Verify credentials and database name
# - Check PostgreSQL pg_hba.conf for connection rules
